---
kind: pipeline
name: build

platform:
  os: linux
  arch: arm

steps:
- name: build
  pull: always
  image: xoxys/rpmbuild-centos7
  commands:
  - ls -l
  environment:
    NODE_VERSION: 10.15.3

...
